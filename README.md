# Flow Cleaning - Web Application

Residential and commercial cleaning web application.

## Run Locally

1. Install dependencies:
   `npm install` or `bun install`
2. Run the dev server:
   `npm run dev` or `bun run dev`
